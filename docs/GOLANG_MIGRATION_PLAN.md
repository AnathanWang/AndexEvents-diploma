# План миграции на Go + Микросервисы

**Проект:** AndexEvents  
**Автор:** Nathan Karma  
**Дата начала:** 26 января 2026  
**Дедлайн:** конец марта 2026 (~2 месяца)  
**Цель:** Переписать бэкенд на Go, разбить на микросервисы, завершить MVP

---

## Содержание

1. [Текущее состояние](#1-текущее-состояние)
2. [Целевая архитектура](#2-целевая-архитектура)
3. [Микросервисы — детальное описание](#3-микросервисы--детальное-описание)
4. [Технологический стек](#4-технологический-стек)
5. [Структура проекта](#5-структура-проекта)
6. [План по неделям](#6-план-по-неделям)
7. [База данных](#7-база-данных)
8. [API контракты](#8-api-контракты)
9. [Деплой и инфраструктура](#9-деплой-и-инфраструктура)
10. [Чеклисты готовности](#10-чеклисты-готовности)

---

## 1. Текущее состояние

### Что есть сейчас (Node.js бэкенд):

| Компонент | Статус | Описание |
|-----------|--------|----------|
| Auth | ✅ Работает | Supabase Auth (будет Firebase) |
| Events CRUD | ✅ Работает | Создание, редактирование, удаление |
| Events Geo | ✅ Работает | PostGIS поиск по радиусу |
| Participants | ✅ Работает | Участие в событиях |
| Matches | ✅ Работает | Like/Dislike/Super-like |
| Friends | ✅ Работает | Запросы в друзья |
| Upload | ✅ Работает | MinIO хранилище |
| Recommendations | ❌ Нет | Нужно реализовать |
| Push Notifications | ❌ Нет | Нужно реализовать (FCM) |

### Flutter приложение:

| Функция | Статус |
|---------|--------|
| Авторизация | ✅ Работает (Supabase) |
| Онбординг | ✅ Работает |
| Лента событий | ✅ Работает |
| Карта событий | ✅ Работает |
| Создание событий | ✅ Работает |
| Профиль | ✅ Работает |
| Матчинг (swipe) | ✅ Работает |
| Друзья | ⚠️ Частично |
| Push уведомления | ❌ Нет |
| Deep links | ❌ Нет |

---

## 2. Целевая архитектура

```
                    ┌─────────────────────────────────────┐
                    │           Flutter App               │
                    │    (iOS / Android / Web)            │
                    └─────────────────┬───────────────────┘
                                      │ HTTPS
                                      ▼
                    ┌─────────────────────────────────────┐
                    │          API Gateway                │
                    │      (Traefik / Nginx)              │
                    │         :80 / :443                  │
                    │                                     │
                    │  • SSL Termination                  │
                    │  • Rate Limiting                    │
                    │  • Request Routing                  │
                    │  • CORS                             │
                    └─────────────────┬───────────────────┘
                                      │
          ┌───────────────┬───────────┴───────────┬───────────────┐
          │               │                       │               │
          ▼               ▼                       ▼               ▼
   ┌─────────────┐ ┌─────────────┐       ┌─────────────┐ ┌─────────────┐
   │    Auth     │ │   Events    │       │    Match    │ │   Upload    │
   │   Service   │ │   Service   │       │   Service   │ │   Service   │
   │   :8001     │ │   :8002     │       │   :8003     │ │   :8004     │
   └──────┬──────┘ └──────┬──────┘       └──────┬──────┘ └──────┬──────┘
          │               │                     │               │
          │               │                     │               │
          ▼               ▼                     ▼               ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │                        PostgreSQL + PostGIS                      │
   │                            :5432                                 │
   └─────────────────────────────────────────────────────────────────┘
                                      │
          ┌───────────────┬───────────┴───────────┬───────────────┐
          │               │                       │               │
          ▼               ▼                       ▼               ▼
   ┌─────────────┐ ┌─────────────┐       ┌─────────────┐ ┌─────────────┐
   │    MinIO    │ │    Redis    │       │  Firebase   │ │   Sentry    │
   │  :9000/9001 │ │    :6379    │       │   (Auth)    │ │ (Monitoring)│
   └─────────────┘ └─────────────┘       └─────────────┘ └─────────────┘
```

### Принципы архитектуры:

1. **Один сервис = один домен** — чёткое разделение ответственности
2. **Shared database** — на этапе MVP все сервисы используют одну БД (упрощает разработку)
3. **REST API** — между клиентом и сервисами
4. **gRPC** — между сервисами (опционально, на будущее)
5. **Stateless** — сервисы не хранят состояние, можно масштабировать

---

## 3. Микросервисы — детальное описание

### 3.1 Auth Service (Сервис авторизации)

**Порт:** 8001  
**Ответственность:** Аутентификация, управление пользователями

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/auth/verify` | POST | Верификация Firebase токена |
| `/users` | POST | Создание пользователя |
| `/users/me` | GET | Текущий профиль |
| `/users/me` | PUT | Обновление профиля |
| `/users/me/location` | PUT | Обновление геолокации |
| `/users/:id` | GET | Профиль по ID |
| `/users/potential-matches` | GET | Список для матчинга |
| `/health` | GET | Health check |

**Зависимости:**
- Firebase Admin SDK (верификация токенов)
- PostgreSQL (таблица `users`)
- Redis (кеш профилей)

**Таблицы БД:**
```sql
users (
  id UUID PRIMARY KEY,
  firebase_uid VARCHAR UNIQUE,
  email VARCHAR UNIQUE,
  name VARCHAR,
  bio TEXT,
  photo_url VARCHAR,
  birth_date DATE,
  gender VARCHAR,
  interests TEXT[],
  location_geo GEOGRAPHY(Point, 4326),
  location_name VARCHAR,
  role user_role DEFAULT 'USER',
  is_verified BOOLEAN DEFAULT false,
  last_active_at TIMESTAMP,
  created_at TIMESTAMP,
  updated_at TIMESTAMP
)
```

---

### 3.2 Events Service (Сервис событий)

**Порт:** 8002  
**Ответственность:** События, участники, рекомендации

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/events` | POST | Создание события |
| `/events` | GET | Список событий (фильтры, гео) |
| `/events/recommended` | GET | Рекомендованные события |
| `/events/user/:userId` | GET | События пользователя |
| `/events/:id` | GET | Детали события |
| `/events/:id` | PUT | Обновление события |
| `/events/:id` | DELETE | Удаление события |
| `/events/:id/join` | POST | Участие в событии |
| `/events/:id/leave` | DELETE | Отмена участия |
| `/events/:id/participants` | GET | Список участников |
| `/health` | GET | Health check |

**Зависимости:**
- PostgreSQL + PostGIS (геопоиск)
- Auth Service (верификация пользователя)
- Redis (кеш событий)

**Таблицы БД:**
```sql
events (
  id UUID PRIMARY KEY,
  title VARCHAR NOT NULL,
  description TEXT,
  photo_url VARCHAR,
  location_geo GEOGRAPHY(Point, 4326),
  location_name VARCHAR,
  address VARCHAR,
  starts_at TIMESTAMP NOT NULL,
  ends_at TIMESTAMP,
  max_participants INTEGER,
  min_age INTEGER,
  max_age INTEGER,
  tags TEXT[],
  status event_status DEFAULT 'PENDING',
  creator_id UUID REFERENCES users(id),
  created_at TIMESTAMP,
  updated_at TIMESTAMP
)

participants (
  id UUID PRIMARY KEY,
  event_id UUID REFERENCES events(id),
  user_id UUID REFERENCES users(id),
  status participant_status,
  joined_at TIMESTAMP,
  UNIQUE(event_id, user_id)
)
```

**Алгоритм рекомендаций:**
```sql
SELECT e.*, 
  -- Совпадение интересов (0-1)
  COALESCE(
    (SELECT COUNT(*)::float FROM unnest(e.tags) t 
     WHERE t = ANY(:user_interests)) / NULLIF(array_length(e.tags, 1), 0),
    0
  ) AS interest_score,
  
  -- Расстояние в км
  ST_Distance(e.location_geo, ST_Point(:lng, :lat)::geography) / 1000 AS distance_km,
  
  -- Дней до события
  EXTRACT(EPOCH FROM (e.starts_at - NOW())) / 86400 AS days_until

FROM events e
WHERE e.status = 'APPROVED'
  AND e.starts_at > NOW()
  AND ST_DWithin(e.location_geo, ST_Point(:lng, :lat)::geography, :radius_meters)
  AND (e.min_age IS NULL OR :user_age >= e.min_age)
  AND (e.max_age IS NULL OR :user_age <= e.max_age)
ORDER BY 
  interest_score DESC,
  distance_km ASC,
  days_until ASC
LIMIT :limit OFFSET :offset;
```

---

### 3.3 Match Service (Сервис матчинга)

**Порт:** 8003  
**Ответственность:** Матчинг, друзья, уведомления

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/matches` | GET | Взаимные матчи |
| `/matches/history` | GET | История действий |
| `/matches/like/:userId` | POST | Лайк |
| `/matches/dislike/:userId` | POST | Дизлайк |
| `/matches/superlike/:userId` | POST | Супер-лайк |
| `/friends` | GET | Список друзей |
| `/friends/requests` | GET | Входящие запросы |
| `/friends/request/:userId` | POST | Отправить запрос |
| `/friends/request/:userId` | DELETE | Отменить запрос |
| `/friends/accept/:requestId` | POST | Принять |
| `/friends/decline/:requestId` | POST | Отклонить |
| `/friends/status/:userId` | GET | Статус дружбы |
| `/notifications` | GET | Уведомления |
| `/notifications/:id/read` | POST | Прочитать |
| `/health` | GET | Health check |

**Зависимости:**
- PostgreSQL (матчи, друзья, уведомления)
- Firebase Admin SDK (push через FCM)
- Auth Service (данные пользователей)
- Redis (очередь уведомлений)

**Таблицы БД:**
```sql
matches (
  id UUID PRIMARY KEY,
  user1_id UUID REFERENCES users(id),
  user2_id UUID REFERENCES users(id),
  user1_action match_action,
  user2_action match_action,
  is_mutual BOOLEAN DEFAULT false,
  matched_at TIMESTAMP,
  created_at TIMESTAMP,
  updated_at TIMESTAMP,
  UNIQUE(user1_id, user2_id)
)

friendships (
  id UUID PRIMARY KEY,
  user1_id UUID REFERENCES users(id),
  user2_id UUID REFERENCES users(id),
  created_at TIMESTAMP,
  UNIQUE(user1_id, user2_id),
  CHECK (user1_id < user2_id)
)

friend_requests (
  id UUID PRIMARY KEY,
  from_user_id UUID REFERENCES users(id),
  to_user_id UUID REFERENCES users(id),
  status friend_request_status DEFAULT 'PENDING',
  created_at TIMESTAMP,
  responded_at TIMESTAMP
)

notifications (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  type notification_type,
  title VARCHAR,
  body TEXT,
  data JSONB,
  is_read BOOLEAN DEFAULT false,
  created_at TIMESTAMP
)
```

**Push уведомления (FCM):**
```go
type NotificationPayload struct {
    Title string            `json:"title"`
    Body  string            `json:"body"`
    Data  map[string]string `json:"data"`
}

// Типы уведомлений
const (
    NotifNewMatch       = "NEW_MATCH"
    NotifFriendRequest  = "FRIEND_REQUEST"
    NotifFriendAccepted = "FRIEND_ACCEPTED"
    NotifEventReminder  = "EVENT_REMINDER"
    NotifNewParticipant = "NEW_PARTICIPANT"
)
```

---

### 3.4 Upload Service (Сервис загрузки)

**Порт:** 8004  
**Ответственность:** Загрузка и обработка файлов

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/upload` | POST | Загрузка файла |
| `/upload/presigned` | GET | Presigned URL для прямой загрузки |
| `/files/:bucket/:path` | DELETE | Удаление файла |
| `/health` | GET | Health check |

**Зависимости:**
- MinIO (S3-совместимое хранилище)
- Auth Service (верификация пользователя)

**Бакеты MinIO:**
| Бакет | Доступ | Назначение |
|-------|--------|------------|
| `avatars` | Public read | Фото профилей |
| `events` | Public read | Фото событий |
| `media` | Private | Прочие файлы |

**Валидация файлов:**
```go
var AllowedMimeTypes = map[string]bool{
    "image/jpeg": true,
    "image/png":  true,
    "image/webp": true,
    "image/gif":  true,
}

var MaxFileSizes = map[string]int64{
    "avatars": 5 * 1024 * 1024,  // 5MB
    "events":  10 * 1024 * 1024, // 10MB
}

// Проверка magic bytes
var FileSignatures = map[string][]byte{
    "image/jpeg": {0xFF, 0xD8, 0xFF},
    "image/png":  {0x89, 0x50, 0x4E, 0x47},
    "image/gif":  {0x47, 0x49, 0x46},
}
```

---

## 4. Технологический стек

### Backend (Go):

| Компонент | Технология | Почему |
|-----------|------------|--------|
| Web Framework | **Gin** | Быстрый, популярный, хорошая документация |
| ORM / SQL | **sqlc** | Type-safe SQL, быстрее GORM |
| Migrations | **golang-migrate** | Стандарт индустрии |
| Config | **Viper** | Env, yaml, json конфиги |
| Logging | **Zap** | Структурированные логи, быстрый |
| Validation | **go-playground/validator** | Валидация структур |
| JWT | **golang-jwt** | Стандартная библиотека |
| Firebase | **firebase-admin-go** | Официальный SDK |
| MinIO | **minio-go** | Официальный SDK |
| PostgreSQL | **pgx** | Быстрый драйвер + PostGIS |
| Redis | **go-redis** | Официальный клиент |
| Testing | **testify** | Assertions, mocks |

### Infrastructure:

| Компонент | Технология |
|-----------|------------|
| Container | Docker |
| Orchestration | Docker Compose (dev), Kubernetes (prod) |
| API Gateway | Traefik |
| Database | PostgreSQL 16 + PostGIS 3.4 |
| Cache | Redis 7 |
| Storage | MinIO |
| Monitoring | Prometheus + Grafana |
| Logging | Loki (или ELK) |
| Errors | Sentry |

### Frontend (без изменений):

| Компонент | Технология |
|-----------|------------|
| Framework | Flutter 3.9+ |
| State | BLoC |
| Auth | Firebase Auth |
| Maps | Yandex MapKit |
| Push | Firebase Cloud Messaging |

---

## 5. Структура проекта

```
andexevents/
├── services/                    # Go микросервисы
│   ├── auth-service/
│   │   ├── cmd/
│   │   │   └── main.go         # Точка входа
│   │   ├── internal/
│   │   │   ├── config/         # Конфигурация
│   │   │   │   └── config.go
│   │   │   ├── handler/        # HTTP handlers
│   │   │   │   ├── auth.go
│   │   │   │   ├── user.go
│   │   │   │   └── health.go
│   │   │   ├── service/        # Бизнес-логика
│   │   │   │   ├── auth.go
│   │   │   │   └── user.go
│   │   │   ├── repository/     # Работа с БД
│   │   │   │   └── user.go
│   │   │   ├── middleware/     # Middleware
│   │   │   │   ├── auth.go
│   │   │   │   └── logging.go
│   │   │   └── model/          # Модели данных
│   │   │       └── user.go
│   │   ├── pkg/                # Переиспользуемый код
│   │   ├── migrations/         # SQL миграции
│   │   ├── Dockerfile
│   │   ├── go.mod
│   │   └── go.sum
│   │
│   ├── events-service/
│   │   └── ... (аналогичная структура)
│   │
│   ├── match-service/
│   │   └── ...
│   │
│   └── upload-service/
│       └── ...
│
├── shared/                      # Общий код между сервисами
│   ├── pkg/
│   │   ├── database/           # Подключение к БД
│   │   ├── firebase/           # Firebase клиент
│   │   ├── logger/             # Логирование
│   │   ├── response/           # Стандартные ответы API
│   │   └── validator/          # Валидация
│   └── go.mod
│
├── deployments/                 # Конфиги для деплоя
│   ├── docker/
│   │   └── docker-compose.yml
│   ├── k8s/                    # Kubernetes манифесты (на будущее)
│   └── traefik/
│       └── traefik.yml
│
├── scripts/                     # Скрипты
│   ├── migrate.sh              # Запуск миграций
│   ├── generate-sqlc.sh        # Генерация sqlc
│   └── dev.sh                  # Запуск dev окружения
│
├── lib/                        # Flutter приложение
├── ios/
├── android/
├── web/
├── backend/                    # Старый Node.js бэкенд (legacy)
├── docs/
└── README.md
```

---

## 6. План по неделям

### Неделя 1 (27 января — 2 февраля): Фундамент

**Цели:**
- [ ] Изучить основы Go (если нужно освежить)
- [ ] Настроить структуру проекта
- [ ] Создать shared пакеты
- [ ] Настроить Docker Compose для разработки

**Задачи:**

| День | Задача | Часы |
|------|--------|------|
| Пн | Go Tour, синтаксис, горутины | 4 |
| Пн | Изучить Gin framework (docs + примеры) | 2 |
| Вт | Создать структуру services/ | 2 |
| Вт | Настроить shared/pkg (logger, response, validator) | 4 |
| Ср | Настроить sqlc, написать первые queries | 4 |
| Ср | Настроить golang-migrate | 2 |
| Чт | Docker Compose для Go сервисов | 3 |
| Чт | Traefik API Gateway базовая настройка | 3 |
| Пт | Тестирование инфраструктуры | 4 |
| Сб | Буфер / рефакторинг | 4 |

**Результат:** Работающая инфраструктура, можно запускать сервисы локально

---

### Неделя 2 (3-9 февраля): Auth Service

**Цели:**
- [ ] Полностью рабочий Auth Service
- [ ] Интеграция с Firebase
- [ ] Миграция Flutter на Firebase Auth

**Задачи:**

| День | Задача | Часы |
|------|--------|------|
| Пн | Firebase проект, получить credentials | 1 |
| Пн | Auth Service: структура, config | 3 |
| Вт | Auth Service: middleware верификации токена | 4 |
| Вт | Auth Service: user repository (sqlc) | 2 |
| Ср | Auth Service: user service + handlers | 4 |
| Ср | Auth Service: тесты | 2 |
| Чт | Flutter: замена Supabase Auth → Firebase Auth | 6 |
| Пт | Flutter: тестирование авторизации | 4 |
| Сб | Исправление багов, рефакторинг | 4 |

**Результат:** Авторизация работает через Firebase + Go

---

### Неделя 3 (10-16 февраля): Events Service

**Цели:**
- [ ] Полностью рабочий Events Service
- [ ] PostGIS геопоиск
- [ ] Рекомендации событий

**Задачи:**

| День | Задача | Часы |
|------|--------|------|
| Пн | Events Service: структура, миграции | 3 |
| Пн | Events Service: repository (CRUD) | 3 |
| Вт | Events Service: PostGIS запросы | 4 |
| Вт | Events Service: service layer | 2 |
| Ср | Events Service: handlers (все endpoints) | 5 |
| Чт | Events Service: рекомендации (алгоритм) | 4 |
| Чт | Events Service: тесты | 2 |
| Пт | Flutter: переключение на новый Events API | 4 |
| Сб | Тестирование, фикс багов | 4 |

**Результат:** События работают через Go, есть рекомендации

---

### Неделя 4 (17-23 февраля): Match Service

**Цели:**
- [ ] Полностью рабочий Match Service
- [ ] Система друзей
- [ ] Push уведомления (FCM)

**Задачи:**

| День | Задача | Часы |
|------|--------|------|
| Пн | Match Service: структура, миграции | 3 |
| Пн | Match Service: match repository | 3 |
| Вт | Match Service: friend repository | 3 |
| Вт | Match Service: service layer | 3 |
| Ср | Match Service: handlers | 4 |
| Ср | FCM интеграция (Firebase Admin SDK) | 2 |
| Чт | Notification service (отправка push) | 4 |
| Чт | Match Service: тесты | 2 |
| Пт | Flutter: переключение на новый Match API | 4 |
| Сб | Flutter: интеграция FCM (получение push) | 4 |

**Результат:** Матчинг + друзья + push уведомления

---

### Неделя 5 (24 февраля — 2 марта): Upload Service + Интеграция

**Цели:**
- [ ] Полностью рабочий Upload Service
- [ ] Все сервисы работают вместе
- [ ] Flutter полностью на новом бэкенде

**Задачи:**

| День | Задача | Часы |
|------|--------|------|
| Пн | Upload Service: структура | 2 |
| Пн | Upload Service: MinIO интеграция | 3 |
| Вт | Upload Service: валидация, handlers | 4 |
| Вт | Upload Service: тесты | 2 |
| Ср | Flutter: переключение на новый Upload API | 3 |
| Ср | Интеграционное тестирование всех сервисов | 3 |
| Чт | Фикс багов интеграции | 6 |
| Пт | Оптимизация, кеширование Redis | 4 |
| Сб | Документация API (OpenAPI/Swagger) | 4 |

**Результат:** Все 4 сервиса работают, Flutter использует новый бэкенд

---

### Неделя 6 (3-9 марта): Flutter — доработка функций

**Цели:**
- [ ] Дописать недостающие функции Flutter
- [ ] Шаринг событий
- [ ] Улучшения UX

**Задачи:**

| День | Задача | Часы |
|------|--------|------|
| Пн | Deep links (шаринг событий) | 4 |
| Вт | Восстановление пароля (Firebase) | 3 |
| Вт | Улучшение экрана друзей | 3 |
| Ср | Улучшение профиля (социальные ссылки) | 4 |
| Чт | Улучшение уведомлений (in-app) | 4 |
| Пт | Offline режим (базовый) | 4 |
| Сб | UI полировка, анимации | 4 |

**Результат:** Полноценное MVP приложение

---

### Неделя 7 (10-16 марта): Админ-панель + Модерация

**Цели:**
- [ ] Простая админ-панель (веб)
- [ ] Модерация событий
- [ ] Модерация пользователей

**Задачи:**

| День | Задача | Часы |
|------|--------|------|
| Пн | Выбор стека (React / Vue / Go templates) | 2 |
| Пн | Базовая структура админки | 4 |
| Вт | Список событий на модерацию | 4 |
| Ср | Approve / Reject события | 4 |
| Чт | Список пользователей | 3 |
| Чт | Бан / разбан пользователей | 3 |
| Пт | Dashboard (статистика) | 4 |
| Сб | Тестирование админки | 4 |

**Результат:** Работающая админ-панель для модерации

---

### Неделя 8 (17-23 марта): Деплой + Production

**Цели:**
- [ ] Деплой на сервер
- [ ] SSL сертификаты
- [ ] Мониторинг
- [ ] CI/CD

**Задачи:**

| День | Задача | Часы |
|------|--------|------|
| Пн | Аренда VPS (Timeweb / Selectel) | 1 |
| Пн | Настройка сервера (Docker, firewall) | 3 |
| Вт | Docker Compose для production | 4 |
| Ср | Traefik + Let's Encrypt SSL | 4 |
| Чт | Prometheus + Grafana мониторинг | 4 |
| Пт | GitHub Actions CI/CD | 4 |
| Сб | Sentry для отслеживания ошибок | 2 |
| Сб | Финальное тестирование | 4 |

**Результат:** Приложение в production!

---

### Неделя 9 (24-31 марта): Буфер + Полировка

**Цели:**
- [ ] Исправление багов
- [ ] Оптимизация производительности
- [ ] Документация
- [ ] Подготовка к релизу

---

## 7. База данных

### Миграции

Все миграции хранятся в `services/{service}/migrations/`:

```
migrations/
├── 000001_init_users.up.sql
├── 000001_init_users.down.sql
├── 000002_init_events.up.sql
├── 000002_init_events.down.sql
└── ...
```

### Индексы

```sql
-- Геопоиск (КРИТИЧНО для производительности)
CREATE INDEX idx_events_location_geo ON events USING GIST (location_geo);
CREATE INDEX idx_users_location_geo ON users USING GIST (location_geo);

-- Поиск событий
CREATE INDEX idx_events_starts_at ON events (starts_at);
CREATE INDEX idx_events_status ON events (status);
CREATE INDEX idx_events_creator_id ON events (creator_id);

-- Матчинг
CREATE INDEX idx_matches_user1_id ON matches (user1_id);
CREATE INDEX idx_matches_user2_id ON matches (user2_id);
CREATE INDEX idx_matches_is_mutual ON matches (is_mutual) WHERE is_mutual = true;

-- Друзья
CREATE INDEX idx_friendships_user1_id ON friendships (user1_id);
CREATE INDEX idx_friendships_user2_id ON friendships (user2_id);
```

---

## 8. API контракты

### Стандартный формат ответа

**Успех:**
```json
{
  "success": true,
  "data": { ... },
  "meta": {
    "page": 1,
    "limit": 20,
    "total": 100
  }
}
```

**Ошибка:**
```json
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Invalid input",
    "details": [
      { "field": "email", "message": "Invalid email format" }
    ]
  }
}
```

### Коды ошибок

| Код | HTTP | Описание |
|-----|------|----------|
| `UNAUTHORIZED` | 401 | Требуется авторизация |
| `FORBIDDEN` | 403 | Нет прав доступа |
| `NOT_FOUND` | 404 | Ресурс не найден |
| `VALIDATION_ERROR` | 400 | Ошибка валидации |
| `CONFLICT` | 409 | Конфликт (дубликат) |
| `INTERNAL_ERROR` | 500 | Внутренняя ошибка |

---

## 9. Деплой и инфраструктура

### Docker Compose (Production)

```yaml
version: '3.8'

services:
  traefik:
    image: traefik:v3.0
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./traefik:/etc/traefik

  auth-service:
    build: ./services/auth-service
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - FIREBASE_CREDENTIALS=${FIREBASE_CREDENTIALS}
    labels:
      - "traefik.http.routers.auth.rule=PathPrefix(`/api/auth`) || PathPrefix(`/api/users`)"

  events-service:
    build: ./services/events-service
    environment:
      - DATABASE_URL=${DATABASE_URL}
    labels:
      - "traefik.http.routers.events.rule=PathPrefix(`/api/events`)"

  match-service:
    build: ./services/match-service
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - FIREBASE_CREDENTIALS=${FIREBASE_CREDENTIALS}
    labels:
      - "traefik.http.routers.match.rule=PathPrefix(`/api/matches`) || PathPrefix(`/api/friends`)"

  upload-service:
    build: ./services/upload-service
    environment:
      - MINIO_ENDPOINT=${MINIO_ENDPOINT}
      - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY}
      - MINIO_SECRET_KEY=${MINIO_SECRET_KEY}
    labels:
      - "traefik.http.routers.upload.rule=PathPrefix(`/api/upload`)"

  postgres:
    image: postgis/postgis:16-3.4
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    volumes:
      - minio_data:/data

volumes:
  postgres_data:
  redis_data:
  minio_data:
```

### Рекомендуемый хостинг (Россия)

| Провайдер | Тариф | Цена | Почему |
|-----------|-------|------|--------|
| **Timeweb Cloud** | VDS L | ~1500₽/мес | 4 vCPU, 8GB RAM, хороший uptime |
| **Selectel** | Cloud Server | ~2000₽/мес | Надёжный, DDoS защита |
| **Reg.ru** | VPS | ~1200₽/мес | Бюджетный вариант |

---

## 10. Чеклисты готовности

### Чеклист: Auth Service готов

- [ ] Firebase токены верифицируются
- [ ] CRUD пользователей работает
- [ ] Геолокация обновляется
- [ ] Potential matches возвращаются
- [ ] Health check отвечает
- [ ] Логи пишутся
- [ ] Тесты проходят

### Чеклист: Events Service готов

- [ ] CRUD событий работает
- [ ] Геопоиск работает (PostGIS)
- [ ] Рекомендации возвращаются
- [ ] Участие в событиях работает
- [ ] Фильтры работают
- [ ] Health check отвечает
- [ ] Тесты проходят

### Чеклист: Match Service готов

- [ ] Like/Dislike/Super-like работает
- [ ] Взаимные матчи определяются
- [ ] Друзья добавляются
- [ ] Запросы в друзья работают
- [ ] Push уведомления отправляются
- [ ] Health check отвечает
- [ ] Тесты проходят

### Чеклист: Upload Service готов

- [ ] Файлы загружаются в MinIO
- [ ] Валидация работает
- [ ] Публичные URL возвращаются
- [ ] Удаление файлов работает
- [ ] Health check отвечает
- [ ] Тесты проходят

### Чеклист: Production готов

- [ ] Все сервисы запускаются
- [ ] SSL работает (HTTPS)
- [ ] Домен настроен
- [ ] Мониторинг работает
- [ ] Логи собираются
- [ ] Бэкапы БД настроены
- [ ] CI/CD работает

---

## Ресурсы для изучения

### Go

| Ресурс | Тип | Ссылка |
|--------|-----|--------|
| Go Tour | Интерактивный | https://go.dev/tour/ |
| Effective Go | Документация | https://go.dev/doc/effective_go |
| Go by Example | Примеры | https://gobyexample.com/ |
| Learn Go with Tests | Книга | https://quii.gitbook.io/learn-go-with-tests/ |

### Gin Framework

| Ресурс | Тип | Ссылка |
|--------|-----|--------|
| Gin Docs | Документация | https://gin-gonic.com/docs/ |
| Gin Examples | Примеры | https://github.com/gin-gonic/examples |

### sqlc

| Ресурс | Тип | Ссылка |
|--------|-----|--------|
| sqlc Docs | Документация | https://docs.sqlc.dev/ |
| sqlc Playground | Песочница | https://play.sqlc.dev/ |

### Микросервисы

| Ресурс | Тип |
|--------|-----|
| Building Microservices (Sam Newman) | Книга |
| Microservices Patterns (Chris Richardson) | Книга |

---

**Удачи! 🚀**

*Последнее обновление: 26 января 2026*
