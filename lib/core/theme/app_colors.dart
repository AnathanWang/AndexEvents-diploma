import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Active 3-color palette:
  // light (cool blue): #dce9ff, dark: #414b4e, primary: #0961f6
  static const Color primary = Color(0xFF0961F6);
  static const Color dark = Color(0xFF414B4E);
  static const Color accent = Color(0xFFDCE9FF);

  static const Color background = Color(0xFFE8F0FF);
  static const Color surface = Color(0xFFE1EBFF);
  static const Color textPrimary = Color(0xFF414B4E);
  static const Color textSecondary = Color(0xFF414B4E);
}